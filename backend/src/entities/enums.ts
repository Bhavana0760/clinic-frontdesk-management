export enum AppointmentStatus {
  BOOKED = "booked",
  COMPLETED = "completed",
  CANCELED = "canceled",
}

export enum QueueStatus {
  WAITING = "waiting",
  WITH_DOCTOR = "with_doctor",
  COMPLETED = "completed",
  SKIPPED = "skipped",
}
